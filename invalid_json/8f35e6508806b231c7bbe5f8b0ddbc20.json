"Here's your new Python coding question:\n\n{\"title\": \"Fibonacci Sequence Generator (Hard)\", \n\"description\": \"Write a function `fibonacci_sequence` that generates the Fibonacci sequence up to a given number. The Fibonacci sequence is a series of numbers where a number is the addition of the last two numbers, starting with 0 and 1.\\n\\n#### Function Signature\\n```python\\ndef fibonacci_sequence(n: int) -> list:\\n```\\n\\n#### Input\\n- `n` (int): The maximum value to generate in the Fibonacci sequence.\\n\\n#### Output\\n- Returns a list of integers representing the Fibonacci sequence up to `n`. For example, if `n` is 10, the output should be `[0, 1, 1, 2, 3, 5, 8]`.\\n\", \n\"code\": \"def fibonacci_sequence(n: int) -> list:\\n    fib_seq = [0, 1]\\n    while fib_seq[-1] <= n:\\n        fib_seq.append(fib_seq[-1] + fib_seq[-2])\\n    return fib_seq[:len(fib_seq)-1]\\n\", \n\"tests\": [\"assert len(fibonacci_sequence(10)) == 6\", \"assert all(i in fibonacci_sequence(20) for i in [0, 1, 1, 2, 3, 5, 8])\", \"assert set(fibonacci_sequence(25)) != {0}\"]}"