" {\n       \"title\": \"'Anagram Checker (Hard)',\n       \"description\": 'Write a function `is_anagram` that checks if two given strings are anagrams of each other.\\n\\n#### Function Signature\\n```python\\ndef is_anagram(str1: str, str2: str) -> bool:\\n```\\n\\n#### Input\\n- `str1`, `str2` (str): Two strings that may contain letters and spaces only.\\n\\n#### Output\\n- Returns `True` if the two input strings are anagrams of each other, `False` otherwise. Two strings are considered anagrams if they have the same number of characters and the characters in one string can be rearranged to form the other string (ignoring case).\\n',\n       \"code\": \"def is_anagram(str1: str, str2: str) -> bool:\\n    # Remove spaces and convert to lowercase\\n    str1 = ''.join([char for char in str1.lower() if char not in [' ', '\\t', '\\n']])\\n    str2 = ''.join([char for char in str2.lower() if char not in [' ', '\\t', '\\n']])\\n    # Check if the two strings have the same number of characters\\n    if len(str1) != len(str2):\\n        return False\\n    \\n    # Count the frequency of each character in both strings\\n    char_counts = Counter(str1)\\n    for char in str2:\\n        if char not in char_counts or char_counts[char] < charCounts[char]:\\n            return False\\n    \\n    # If no characters were found that don't match, the strings are anagrams\\n    return True\",\n       \"tests\": [\"assert is_anagram('Listen', 'Silent') == True\", \"assert is_anagram('Hello', 'world') == False\", \"assert is_anagram('Madam', 'Madam') == True\", \"assert is_anagram('Welcome', 'elcomNecilew') == True\"]\n   }"