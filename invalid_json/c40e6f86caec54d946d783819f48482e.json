"Here's your requested Python coding question:\n\n{\"title\": \"Matrix Chain Multiplication\", \"description\": \"Given a list of matrices and their dimensions, write a function that calculates the minimum number of scalar multiplications required to multiply all these matrices together. This problem is known as the Matrix Chain Multiplication problem.\\n\\n#### Function Signature\\n```python\\ndef min_scalar_multiplications(matrices: List[List[int]], dims: List[Tuple[int, int]]) -> int:\\n```\\n\\n#### Input\\n- `matrices`: A list of integers representing the number of matrices.\\n- `dims`: A list of tuples where each tuple contains two integers representing the dimensions (rows and columns) of a matrix.\\n\\n#### Output\\n- Returns the minimum number of scalar multiplications required to multiply all these matrices together.\\n\", \"code\": \"\nfrom typing import List, Tuple\n\ndef min_scalar_multiplications(matrices: List[List[int]], dims: List[Tuple[int, int]]) -> int:\n    n = len(matrices)\n    dp = [[0] * n for _ in range(n)]\n    \n    for chain_length in range(2, n+1):\n        i = 0\n        while i < n - chain_length + 1:\n            j = i + chain_length - 1\n            dp[i][j] = float('inf')\n            k = i\n            while k <= j:\n                if k == i:\n                    dp[i][j] = dims[k][0] * dims[k][1] * dp[k+1][j]\n                elif k == j:\n                    dp[i][j] = dims[k][0] * dims[k][1] * dp[i][k-1]\n                else:\n                    dp[i][j] = min(dp[i][k-1] + dims[i][0] * dims[k][1] * dims[k+1][0] * dims[j][1], \n                                   dp[k][j] + dims[i][0] * dims[k][1] * dims[i][1] * dims[j][1])\n                k += 1\n            i += 1\n    \n    return dp[0][n-1]\n\", \"tests\": [\"assert min_scalar_multiplications([[3, 4], [5]], [(2, 2), (2, 3)]) == 11\", \"assert min_scalar_multiplications([[2, 3], [3, 1], [1, 2]], [(2, 2), (3, 1), (1, 2)]) == 7\", \"assert min_scalar_multiplications([[5, 6], [7, 8], [9, 10], [11, 12]], [(2, 3), (3, 4), (4, 5), (5, 6)]) == 14\"]}"