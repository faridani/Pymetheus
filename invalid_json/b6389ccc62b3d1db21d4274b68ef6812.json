"Here's the Python coding question for you:\n\n{\"title\": \"Easy: Calculate the Difference\", \"description\": \"Write a function that calculates the difference between two lists of integers. The function should take as input two lists, each containing any number of integers, and return the absolute value of the difference between the maximum element in the first list and the minimum element in the second list.\\n\\n#### Function Signature\\n```python\\ndef calculate_difference(list1: list[int], list2: list[int]) -> int:\\n```\\n\\n#### Input\\n- `list1` (list[int]): A list of integers.\\n- `list2` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the absolute value of the difference between the maximum element in `list1` and the minimum element in `list2`, as an integer.\\n\", \"code\": \"import math\\ndef calculate_difference(list1: list[int], list2: list[int]) -> int:\\n    # Calculate the maximum element in list1\\n    max_in_list1 = max(list1)\\n    # Calculate the minimum element in list2\\n    min_in_list2 = min(list2)\\n    # Calculate and return the absolute difference\\n    return abs(max_in_list1 - min_in_list2)\", \"tests\": [\"assert calculate_difference([1, 3, 5], [2, 4]) == 3\", \"assert calculate_difference([10, 20, 30], [40, 50]) == 30\", \"assert calculate_difference([-10, -20, -30], [-40, -50]) == 40\", \"assert calculate_difference([1], []) == 1\"]}"