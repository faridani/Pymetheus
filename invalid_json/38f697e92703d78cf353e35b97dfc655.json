"Here's your easy python coding question:\n\n{\"title\": \"Easy: Counting Duplicates\", \"description\": \"Write a function `count_duplicates` that takes a list of integers as input and returns the count of elements that are duplicates (i.e., occur more than once) in the list.\\n\\n#### Function Signature\\n```python\\ndef count_duplicates(lst: list[int]) -> int:\\n```\\n\\n#### Input\\n- `lst` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the number of elements that are duplicates in the input list.\", \"code\": \"def count_duplicates(lst: list[int]) -> int:\\n    frequency_dict = {}\\n    for num in lst:\\n        if num not in frequency_dict:\\n            frequency_dict[num] = 1\\n        else:\\n            frequency_dict[num] += 1\\n    duplicates_count = sum(1 for count in frequency_dict.values() if count > 1)\\n    return duplicates_count\", \"tests\": [\"assert count_duplicates([1, 2, 3]) == 0\", \"assert count_duplicates([1, 2, 3, 4, 5]) == 0\", \"assert count_duplicates([1, 2, 3, 2, 4]) == 1\", \"assert count_duplicates([1, 2, 3, 2, 2]) == 2\"]}"