"Here is the problem you requested:\n\n{\"title\": \"Euler's Totient Function (Hard)\", \n\"description\": \"The Euler totient function, denoted by \u03c6(n), counts the positive integers up to n that are relatively prime to n. Given two positive integers n and k, write a function `euler_totient` that returns the value of \u03c6(k) mod n.\\n\\n#### Function Signature\\n```python\\ndef euler_totient(n: int, k: int) -> int:\\n```\\n\\n#### Input\\n- `n` (int): The modulus.\\n- `k` (int): The number for which we want to calculate Euler's totient function.\\n\\n#### Output\\n- Returns the value of \u03c6(k) mod n.\\n\", \n\"code\": \"import math\n\ndef euler_totient(n: int, k: int) -> int:\n    def gcd(a: int, b: int) -> int:\n        if b == 0:\n            return a\n        else:\n            return gcd(b, a % b)\n\n    def phi(k: int) -> int:\n        result = k\n        p = 2\n        while p * p <= k:\n            if k % p == 0:\n                while k % p == 0:\n                    k //= p\n                result -= result // p\n            else:\n                p += 1\n        if k > 1:\n            result -= 1\n        return result\n\n    totient = phi(k)\n    return pow(totient, -1, n)\n\n\", \n\"tests\": [\"assert euler_totient(5, 8) == 4\", \"assert euler_totient(7, 11) == 5\", \"assert euler_totient(2, 3) == 0\", \"assert euler_totient(10, 16) == 8\"]}"