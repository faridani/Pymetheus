"Here is your hard python coding question:\n\n{\n\"title\": \"Hard: Finding the First Duplicate\",\n\"description\": \"Given a list of integers, write a function that finds the first duplicate. If there are no duplicates, return None.\\n\\n#### Function Signature\\n```python\\ndef find_first_duplicate(nums: List[int]) -> int | None:\\n```\\n\\n#### Input\\n- `nums` (List[int]): A list of integers.\\n\\n#### Output\\n- Returns the first duplicate integer if found, `None` otherwise.\",\n\"code\": \"\"\"\nfrom typing import List\n\ndef find_first_duplicate(nums: List[int]) -> int | None:\n    seen = set()\n    for num in nums:\n        if num in seen:\n            return num\n        seen.add(num)\n    return None\n\"\"\", \n\"tests\": [\n    \"assert find_first_duplicate([1, 2, 3, 4, 5]) == None\",\n    \"assert find_first_duplicate([1, 2, 3, 4, 5, 2]) == 2\",\n    \"assert find_first_duplicate([1, 1, 2, 3, 4]) == 1\",\n    \"assert find_first_duplicate([]) == None\"\n]\n}"