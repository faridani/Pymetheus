"{'title': 'Hard', 'description': 'Given a list of integers, determine if any number is missing from the range defined by the smallest and largest numbers in the list.\\n\\n#### Function Signature\\n```python\\ndef find_missing_number(nums: List[int]) -> int:\\n```\\n\\n#### Input\\n- `nums` (List[int]): A non-empty list of unique integers.\\n\\n#### Output\\n- Returns the missing number within the range defined by the smallest and largest numbers in the input list.\\n', 'code': \"from typing import List\\n\\ndef find_missing_number(nums: List[int]) -> int:\\n    # Find minimum and maximum values in the list\\n    min_num, max_num = min(nums), max(nums)\\n    missing_numbers = set(range(min_num, max_num + 1)) - set(nums)\\n    return sorted(missing_numbers)[0] if len(missing_numbers) > 0 else -1\", 'tests': ['assert find_missing_number([3, 2, 1, 5]) == 4', 'assert find_missing_number([1, 2, 3]) == -1', 'assert find_missing_number([-9, -7, -8, -6]) == -10']}"