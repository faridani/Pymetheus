"Here's a hard python coding question for you:\n\n```json\n{\n    \"title\": \"Hard: Maximal Rectangle in Histogram\",\n    \"description\": \"Given an array of integers representing the histogram bars, write a function `max_rectangle_area` that returns the maximum possible area of a rectangle within this histogram.\\n\\n#### Function Signature\\n```\\ndef max_rectangle_area(histogram: list[int]) -> int:\\n```\\n\\n#### Input\\n- `histogram` (list[int]): A list of integers representing the heights of bars in the histogram.\\n\\n#### Output\\n- Returns the maximum possible area of a rectangle within this histogram.\\n\",\n    \"code\": \"\"\"\nimport math\n\ndef max_rectangle_area(histogram: list[int]) -> int:\n    # Initialize maximum area\n    max_area = 0\n    \n    # Iterate over each bar in the histogram\n    for i, height in enumerate(histogram):\n        # Initialize minimum height to current bar's height\n        min_height = height\n        \n        # Find the minimum height of all bars to the left and right of the current bar\n        for j in range(i-1, -1, -1):\n            if histogram[j] < min_height:\n                min_height = histogram[j]\n        \n        for k in range(i+1, len(histogram)):\n            if histogram[k] < min_height:\n                min_height = histogram[k]\n        \n        # Calculate area of rectangle with minimum height as the width\n        area = min_height * (i + 1 - i)\n        max_area = max(max_area, area)\n    \n    return max_area\"\"\",\n    \"tests\": [\"assert max_rectangle_area([2,1,5,6,2,3]) == 10\", \"assert max_rectangle_area([2,2,4,2,2]) == 8\"]\n}\n```"