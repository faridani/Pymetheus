" {\n       \"title\": \"Medium: Count Vowels in a String\",\n       \"description\": \"Write a function `count_vowels` that takes a string as input and returns the number of vowels (both lowercase and uppercase) in the string.\\n\\n#### Function Signature\\n```python\\ndef count_vowels(s: str) -> int:\\n```\\n\\n#### Input\\n- `s` (str): A string that may contain letters, numbers, spaces, and punctuation.\\n\\n#### Output\\n- Returns an integer representing the number of vowels in the input string.\",\n       \"code\": \"\"\"def count_vowels(s: str) -> int:\n           # Convert to lowercase for simplicity\n           s = s.lower()\n           # Define a set of vowels\n           vowels = set('aeiou')\n           # Count the number of vowels in the string\n           return sum(1 for char in s if char in vowels)\"\"\",\n       \"tests\": ['assert count_vowels(\"Hello World\") == 3', 'assert count_vowels(\"Python is awesome\") == 4', 'assert count_vowels(\"aeiouAEIOU\") == 10']\n   }"