" {\n      \"title\": \"Uppercase and Lowercase Character Counter\",\n      \"description\": \"Write a function `count_characters` that takes a string as input, counts the number of uppercase and lowercase characters in the string, and returns a dictionary with two keys: `uppercase` and `lowercase`. The function should ignore spaces, punctuation, and special characters.\\n\\n#### Function Signature\\n```python\\ndef count_characters(s: str) -> dict:\\n```\\n\\n#### Input\\n- `s` (str): A string that may contain letters, numbers, spaces, punctuation, and special characters.\\n\\n#### Output\\n- Returns a dictionary with two keys: `uppercase` and `lowercase`, where each key contains an integer representing the count of uppercase and lowercase characters respectively.\\n\",\n      \"code\": \"def count_characters(s: str) -> dict:\\n    # Initialize empty counters\\n    uppercase_count = 0\\n    lowercase_count = 0\\n    \\n    # Iterate through the string, counting uppercase and lowercase characters separately\\n    for char in s:\\n        if char.isupper():\\n            uppercase_count += 1\\n        elif char.islower():\\n            lowercase_count += 1\\n    \\n    # Return a dictionary with the counts\\n    return {'uppercase': uppercase_count, 'lowercase': lowercase_count}\",\n      \"tests\": [\n          'assert count_characters(\"HELLO WORLD\") == {\"uppercase\": 2, \"lowercase\": 5}',\n          'assert count_character(\"123abc!@#$\") == {\"uppercase\": 0, \"lowercase\": 3}'\n      ]\n   }"