" ```json\n{\n  \"title\": \"Unique Element Counter (Medium)\",\n  \"description\": \"Given a list of integers, count the number of unique elements. Unique means that every element appears only once in the list.\\n\\n#### Function Signature\\n```python\\ndef count_unique(numbers: List[int]) -> int:\\n```\\n\\n#### Input\\n- `numbers` (List[int]): A list of integers, where some elements might repeat.\\n\\n#### Output\\n- Returns an integer representing the number of unique elements in the input list.\\n\",\n  \"code\": \"from typing import List\\ndef count_unique(numbers: List[int]) -> int:\\n    return len(set(numbers))\\n\",\n  \"tests\": [\n    \"assert count_uniquec([] == 0)\",\n    \"assert count_unique([1, 2, 3, 4, 5]) == 5\",\n    \"assert count_unique([1, 1, 2, 3, 4, 5]) == 5\",\n    \"assert count_unique([-1, -2, -3, -4, -5]) == 5\",\n    \"assert count_unique([0, 1, 1, 2, 3, 3, 3, 4, 5]) == 6\"\n  ]\n}\n```"