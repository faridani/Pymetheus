"Here's a hard Python coding question for you:\n\n```json\n{\n    \"title\": \"Hard: Maximum Sum of an Unsorted Subarray\",\n    \"description\": \"Given an array of integers, write a function `max_sum_unsorted_subarray` that returns the maximum sum of any unsorted subarray in the given array. An unsorted subarray is any contiguous subset of the original array.\n\n#### Function Signature\n```\ndef max_sum_unsorted_subarray(arr: list[int]) -> int:\n```\n\n#### Input\n- `arr` (list[int]): A list of integers that may contain negative numbers, positive numbers, or zero.\n\n#### Output\n- Returns the maximum sum of any unsorted subarray in the given array.\n\",\n    \"code\": \"\"\"\ndef max_sum_unsorted_subarray(arr: list[int]) -> int:\n    n = len(arr)\n    total_sum = sum(arr)\n    max_sum = float('-inf')\n    \n    for i in range(n):\n        current_sum = 0\n        for j in range(i, n):\n            current_sum += arr[j]\n            if current_sum > max_sum:\n                max_sum = current_sum\n                \n    return max_sum\n\"\"\",\n    \"tests\": [\n        'assert max_sum_unsorted_subarray([-2,3,-4]) == 5',\n        'assert max_sum_unsorted_subarray([1, -2, 3, 10, -4, 7, 2, -5]) == 22',\n        'assert max_sum_unsorted_subarray([1, 2, 3, 4, 5]) == 15',\n        'assert max_sum_unsorted_subarray([-1, 1, -1, 1]) == 2'\n    ]\n}\n```"