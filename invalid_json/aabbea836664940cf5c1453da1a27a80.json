"Here's your Python coding question:\n\n{\"title\": \"Easy: Counting Duplicates\", \"description\": \"Given a list of integers, write a function that counts the number of duplicate values. A duplicate value is one that appears more than once in the list.\\n\\n#### Function Signature\\n```python\\ndef count_duplicates(lst: list[int]) -> int:\\n```\\n\\n#### Input\\n- `lst` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the number of duplicate values in the input list.\\n\", \"code\": \"def count_duplicates(lst: list[int]) -> int:\\n    # Initialize a dictionary to keep track of counts for each value\\n    value_counts = {}\\n    # Iterate over the list and update the counts dictionary\\n    for num in lst:\\n        if num in value_counts:\\n            value_counts[num] += 1\\n        else:\\n            value_counts[num] = 1\\n    # Count the number of values that appear more than once\\n    return sum(1 for count in value_counts.values() if count > 1)\\n\", \"tests\": [\"assert count_duplicates([1, 2, 3]) == 0\", \"assert count_duplicates([1, 2, 2]) == 1\", \"assert count_duplicates([1, 1, 1, 1]) == 3\", \"assert count_duplicates([1, 2, 3, 4, 5]) == 0\"]}"