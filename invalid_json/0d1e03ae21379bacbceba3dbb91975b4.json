" {\n      \"title\": \"Prime Number Checker (Hard)\",\n      \"description\": \"Write a function `is_prime` that checks if a given number is prime.\\n\\n#### Function Signature\\n```python\\ndef is_prime(num: int) -> bool:\\n```\\n\\n#### Input\\n- `num` (int): A non-negative integer.\\n\\n#### Output\\n- Returns `True` if the input number is a prime, `False` otherwise.\\n\",\n      \"code\": \"def is_prime(num: int) -> bool:\\n    # Edge case: 0 and 1 are not prime numbers\\n    if num <= 1: return False\\n    \\n    # Range from 2 to the square root of the number\\n    for i in range(2, int(num ** 0.5) + 1):\\n        # If the number is divisible by any number in this range, it's not prime\\n        if num % i == 0:\\n            return False\\n    \\n    # If we made it through the loop without finding any factors, the number is prime\\n    return True\",\n      \"tests\": [\n          'assert is_prime(2) == True',\n          'assert is_prime(3) == True',\n          'assert is_prime(5) == True',\n          'assert not is_prime(4) == True',\n          'assert not is_prime(6) == True',\n          'assert is_prime(7) == True',\n          'assert is_prime(23) == True',\n          'assert is_prime(197) == True',\n          'assert not is_prime(8) == True',\n          'assert not is_prime(9) == True',\n          'assert not is_prime(10) == True'\n      ]\n   }"