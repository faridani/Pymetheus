"Here's a new problem with the requested format:\n\n```json\n{\n    'title': 'Find the Maximum Subarray',\n    'description': 'Given an integer array `nums`, find the maximum possible sum of a subarray. A subarray is a contiguous part of the array, not necessarily starting from the first element or ending at the last element.\\n\\n#### Function Signature\\n```python\\ndef max_subarray(nums: list[int]) -> int:\\n```\\n\\n#### Input\\n- `nums` (list[int]): An integer list that may contain positive and negative numbers.\\n\\n#### Output\\n- Returns the maximum possible sum of a subarray.',\n    'code': \"def max_subarray(nums: list[int]) -> int:\\n    if not nums:\\n        return 0\\n\\n    current_sum = max_sum = nums[0]\\n    for num in nums[1:]:\\n        current_sum = max(num, current_sum + num)\\n        max_sum = max(max_sum, current_sum)\\n    return max_sum\",\n    'tests': ['assert max_subarray([-2, 1, -3, 4, -1, 2, 1, -5, 4]) == 6',\n             'assert max_subarray([0, 0, 0, 0, 0]) == 0',\n             'assert max_subarray([-1, -2, -3, -4, -5]) == -1']\n}\n```"