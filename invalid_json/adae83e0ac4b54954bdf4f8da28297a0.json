"Here is the generated problem:\n\n{\"title\": \"Hard: Scheduling Meetings\", \n\"description\": \"You are a busy manager who needs to schedule meetings with your team. You have n tasks to complete, and each task requires exactly m minutes of uninterrupted time. Write a function `schedule_meetings` that takes two parameters, `n` (the number of tasks) and `m` (the time required for each task), and returns the minimum number of hours needed to complete all the tasks.\\n\\n#### Function Signature\\n```\\ndef schedule_meetings(n: int, m: int) -> int:\\n```\\n\\n#### Input\\n- `n` (int): The number of tasks to be completed.\\n- `m` (int): The time required for each task in minutes.\\n\\n#### Output\\n- Returns the minimum number of hours needed to complete all the tasks.\\n\", \n\"code\": \"def schedule_meetings(n: int, m: int) -> int:\\n    # Calculate the total time required for all tasks in minutes\\n    total_time = n * m\\n    # Convert the total time from minutes to hours\\n    return total_time // 60\\n\", \n\"tests\": [\"assert schedule_meetings(3, 10) == 1\", \"assert schedule_meetings(4, 20) == 2\", \"assert schedule_meetings(5, 30) == 2\", \"assert schedule_meetings(6, 40) == 2\"]}"