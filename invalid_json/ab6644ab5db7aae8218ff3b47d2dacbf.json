"```json\n{'title': 'Easy: Sum of Multiples', 'description': 'Write a function `sum_of_multiples` that takes an integer `n` and returns the sum of all multiples of 3 or 5 that are less than or equal to `n`. A multiple is a number that can be divided evenly without leaving a remainder.\\n\\n#### Function Signature\\n```python\\ndef sum_of_multiples(n: int) -> int:\\n```\\n\\n#### Input\\n- `n` (int): An integer greater than 0.\\n\\n#### Output\\n- Returns an integer representing the sum of all multiples of 3 or 5 less than or equal to `n`.\\n', 'code': \"def sum_of_multiples(n: int) -> int:\\n    total = 0\\n    for i in range(1, n + 1):\\n        if i % 3 == 0 or i % 5 == 0:\\n            total += i\\n    return total\\n\", 'tests': ['assert sum_of_multiples(10) == 33', 'assert sum_of_multiples(100) == 2338', 'assert sum_of_multiples(1000) == 233168']}\n```"