" {\n       \"title\": \"[Hard] Substring with Maximum Unique Characters\",\n       \"description\": \"Given a string, find the substring with the maximum number of unique characters. Write a function `max_unique_substring` that takes a string as input and returns the substring with the maximum number of unique characters.\\n\\n#### Function Signature\\n```python\\ndef max_unique_substring(s: str) -> str:\\n```\\n\\n#### Input\\n- `s` (str): A string that may contain letters, numbers, spaces, and punctuation.\\n\\n#### Output\\n- Returns the substring with the maximum number of unique characters. If there are multiple such substrings, return the first one.\",\n       \"code\": \"def max_unique_substring(s: str) -> str:\\n    char_index = {}  # Maps each character to its last index in s\\n    start = end = max_len = 0  # Variables to keep track of the maximum length substring\\n    for i, c in enumerate(s):\\n        if c in char_index and char_index[c] >= start:\\n            start = char_index[c] + 1\\n        else:\\n            end = i\\n        char_index[c] = i\\n        max_len = max(max_len, end - start + 1)\\n    return s[start:start+max_len]\",\n       \"tests\": ['assert max_unique_substring(\"abcabcbb\") == \"abc\"', 'assert max_unique_substring(\"pwwkew\") == \"wke\"', 'assert max_unique_substring(\"bbbbb\") == \"b\"', 'assert max_unique_substring(\"dvdf\") == \"vdf\"']\n   }"