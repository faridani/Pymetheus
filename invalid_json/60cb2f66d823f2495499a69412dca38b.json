"Here is a new Python coding question:\n\n{\"title\": \"Fibonacci Sequence with Primes\", \"description\": \"A Fibonacci sequence is a series of numbers where each number is the sum of the two preceding ones, usually starting with 0 and 1. A prime number is a natural number greater than 1 that has no divisors other than 1 and itself.\\n\\nWrite a function `fibonacci_primes` that generates a Fibonacci sequence and returns only the prime numbers in the sequence up to a given limit.\\n\\n#### Function Signature\\n```python\\ndef fibonacci_primes(limit: int) -> list[int]:\\n```\\n\\n#### Input\\n- `limit` (int): The upper bound of the Fibonacci sequence.\\n\\n#### Output\\n- Returns a list of prime numbers in the Fibonacci sequence up to the given limit.\\n\", \"code\": \"import math\n\ndef is_prime(n: int) -> bool:\n    if n <= 1:\n        return False\n    for i in range(2, int(math.sqrt(n)) + 1):\n        if n % i == 0:\n            return False\n    return True\n\ndef fibonacci_primes(limit: int) -> list[int]:\n    a, b = 0, 1\n    primes = []\n    while b <= limit:\n        if is_prime(b):\n            primes.append(b)\n        a, b = b, a + b\n    return primes\\n\", \"tests\": [\"assert len(fibonacci_primes(100)) == 20\", \"assert all(is_prime(n) for n in fibonacci_primes(100))\", \"assert not any(n > 100 for n in fibonacci_primes(100))\"]}\n\nLet me know if you'd like another question!"