"Here is a new Python coding question:\n\n{'title': 'Easy: Finding the First Duplicate', 'description': \"Write a function `first_duplicate` that takes a list of integers as input and returns the first duplicate element it encounters. If no duplicates are found, return None.\\n\\n#### Function Signature\\n```python\\ndef first_duplicate(lst: list[int]) -> int | None:\\n```\\n\\n#### Input\\n- `lst` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the first duplicate element in the input list, or None if no duplicates are found.\", 'code': \"def first_duplicate(lst: list[int]) -> int | None:\\n    seen = set()\\n    for num in lst:\\n        if num in seen:\\n            return num\\n        seen.add(num)\\n    return None\\n\", 'tests': ['assert first_duplicate([1, 2, 3, 4, 5]) is None', 'assert first_duplicate([1, 2, 3, 4, 2]) == 2', 'assert first_duplicate([1, 1, 2, 3]) == 1', 'assert first_duplicate([]) is None']}"