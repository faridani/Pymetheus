"Here's your new Python coding question:\n\n{\"title\": \"Easy: Counting Duplicates\", \n\"description\": \"Write a function `count_duplicates` that takes a list of integers as input and returns the count of duplicate values.\\n\\n#### Function Signature\\n```python\\ndef count_duplicates(nums: list[int]) -> int:\\n```\\n\\n#### Input\\n- `nums` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the number of duplicate values in the input list.\", \n\"code\": \"def count_duplicates(nums: list[int]) -> int:\\n    # Create a dictionary to store the frequency of each number\\n    freq = {}\\n    for num in nums:\\n        if num not in freq:\\n            freq[num] = 1\\n        else:\\n            freq[num] += 1\\n    # Count the duplicate values by summing up the frequencies of numbers with more than one occurrence\\n    return sum(1 for count in freq.values() if count > 1)\", \n\"tests\": [\"assert count_duplicates([1, 2, 3]) == 0\", \"assert count_duplicates([1, 2, 2, 3]) == 1\", \"assert count_duplicates([1, 2, 2, 3, 4, 5, 6, 7, 8, 9]) == 2\", \"assert count_duplicates([1, 2, 2, 2, 3, 3, 3, 4, 5, 5, 5, 5]) == 6\"]}"