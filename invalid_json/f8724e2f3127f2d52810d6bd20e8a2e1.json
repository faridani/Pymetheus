"Here is your new Python coding question:\n\n{\"title\": \"Hard: Matrix Rotation\", \"description\": \"Given an input matrix (2D list of integers), write a function `rotate_matrix` that rotates the matrix 90 degrees clockwise, as long as it's possible to do so. The matrix should be rotated in place.\\n\\n#### Function Signature\\n```python\\ndef rotate_matrix(matrix: List[List[int]]) -> None:\\n```\\n\\n#### Input\\n- `matrix` (List[List[int]]): A 2D list of integers representing the input matrix.\\n\\n#### Output\\n- Rotates the input matrix 90 degrees clockwise in place. If it's not possible to rotate, returns None.\\n\", \"code\": \"\"\"\nimport copy\nfrom typing import List\n\ndef rotate_matrix(matrix: List[List[int]]) -> None:\n    if len(matrix) == 0 or len(matrix[0]) == 0:\n        return None\n    \n    rotated = [list(reversed(i)) for i in zip(*matrix)]\n    \n    matrix.clear()\n    matrix.extend(rotated)\n\"\"\", \"tests\": [\"assert rotate_matrix([[1, 2], [3, 4]]) is None and [[4, 3], [2, 1]] == [[1, 2], [3, 4]]\", \"assert rotate_matrix([[1, 2, 3], [4, 5, 6]]) is None and [[6, 5, 4], [3, 2, 1]] == [[1, 2, 3], [4, 5, 6]]\"]}"