"Here is your Python coding question:\n\n{\"title\": \"Hard: Sum of Distinct Subsequences\", \"description\": \"Given a string `s` and an integer `k`, write a function that returns the sum of all distinct subsequences of length `k`. A subsequence is a sequence that can be derived from another sequence by deleting some or no elements without changing the order of the remaining elements.\\n\\n#### Function Signature\\n```\\ndef subsequence_sum(s: str, k: int) -> int:\\n```\", \"code\": \"\"\"\nimport math\n\ndef subsequence_sum(s: str, k: int) -> int:\n    if k > len(s):\n        return 0\n    s = ''.join(sorted(set(s)))\n    subsequences = set()\n    for i in range(len(s) - k + 1):\n        sub = s[i:i+k]\n        if sub not in subsequences:\n            subsequences.add(sub)\n    total_sum = sum([int(''.join(filter(str.isdigit, sub))) for sub in subsequences])\n    return total_sum\n\"\"\", \"tests\": [\"assert subsequence_sum('123456789', 3) == 20\", \"assert subsequence_sum('abcde', 2) == 0\", \"assert subsequence_sum('1234', 1) == 1\", \"assert subsequence_sum('12345', 5) == 120\"]}"