"Here is your requested question:\n\n{\"title\": \"Hard: Finding the Most Frequent Character\", \n\"description\": \"In this problem, you are required to write a function that finds the most frequent character in an input string. The function should return a tuple containing the character and its frequency.\\n\\n#### Function Signature\\n```python\\ndef most_frequent_character(s: str) -> (str, int):\\n```\\n\\n#### Input\\n- `s` (str): A string that may contain any characters.\\n\\n#### Output\\n- Returns a tuple `(char, freq)` where `char` is the most frequent character in the input string and `freq` is its frequency.\\n\", \n\"code\": \"\nimport collections\n\ndef most_frequent_character(s: str) -> (str, int):\n    char_frequency = collections.Counter(s)\n    max_freq = max(char_frequency.values())\n    most_frequent_chars = [char for char, freq in char_frequency.items() if freq == max_freq]\n    return (most_frequent_chars[0], max_freq)\n\", \n\"tests\": [\"assert most_frequent_character('abbccdee') == ('e', 3)\", \"assert most_frequent_character('aaaabbbbcccdddeee') == ('e', 5)\", \"assert most_frequent_character('abcdefg') == ('a', 1)\", \"assert most_frequent_character('hello world') == ('o', 2)\"]}"