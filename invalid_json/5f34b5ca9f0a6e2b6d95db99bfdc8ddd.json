"Here is the medium-level Python coding question:\n\n{\"title\": \"String Compression\", \"description\": \"Write a function that compresses a string by removing consecutive duplicates and replacing them with a single instance. For example, the input 'aaabbbccc' should be compressed to 'abc'. The output string should still be valid for any given input.\\n\\n#### Function Signature\\n```python\\ndef compress(s: str) -> str:\\n```\\n\\n#### Input\\n- `s` (str): A string containing letters, numbers, or special characters.\\n\\n#### Output\\n- Returns the compressed version of the input string.\\n\", \"code\": \"\"\"\ndef compress(s: str) -> str:\n    compressed = []\n    for char in s:\n        if not compressed or char != compressed[-1]:\n            compressed.append(char)\n    return ''.join(compressed)\n\"\"\", \"tests\": [\"assert compress('aaabbbccc') == 'abc'\", \"assert compress('a') == 'a'\", \"assert compress('') == ''\", \"assert compress('hello world') == 'helo wrd'\"]}"