" {\n    \"title\": \"Valid Parentheses Checker (Medium)\",\n    \"description\": \"Write a function `isValidParentheses` that checks if the given string of parentheses is valid. The input string may only contain '(', ')', '{', '}', '[', ']' and no other characters.\\n\\n#### Function Signature\\n```python\\ndef isValidParentheses(s: str) -> bool:\\n```\\n\\n#### Input\\n- `s` (str): A string that only contains '(', ')', '{', '}', '[', ']'.\\n\\n#### Output\\n- Returns `True` if the input string is valid, `False` otherwise.\\n\",\n    \"code\": \"def isValidParentheses(s: str) -> bool:\\n    stack = []\\n    opens = {'(', '[', '{'}\\n    closes = {')', ']', '}'}\\n    for char in s:\\n        if char in opens:\\n            stack.append(char)\\n        elif char in closes:\\n            if not stack or stack.pop() != closes[char]:\\n                return False\\n    return len(stack) == 0\",\n    \"tests\": [\n        'assert isValidParentheses(\"()\") == True',\n        'assert isValidParentheses(\"()[]{}\") == True',\n        'assert isValidParentheses(\"(]\") == False',\n        'assert isValidParentheses(\"([)]\") == False',\n        'assert isValidParentheses(\"{[(])}\") == False'\n    ]\n}"