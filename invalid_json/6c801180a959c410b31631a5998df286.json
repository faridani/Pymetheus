"Here is a Python coding question that meets the template:\n\n{\n\"title\": \"Hard: Max Consecutive Ones in Binary String\",\n\"description\": \"Write a function `max_consecutive_ones` that takes a binary string (a string consisting only of '0's and '1's) as input and returns the maximum number of consecutive '1's in the string.\\n\\n#### Function Signature\\n```python\\ndef max_consecutive_ones(s: str) -> int:\\n```\\n\\n#### Input\\n- `s` (str): A binary string that may contain leading or trailing zeros.\\n\\n#### Output\\n- Returns the maximum number of consecutive '1's in the input string.\",\n\"code\": \"\"\"\ndef max_consecutive_ones(s: str) -> int:\n    if not s:  # edge case: empty string\n        return 0\n\n    max_count = 0\n    current_count = 0\n\n    for char in s:\n        if char == '1':\n            current_count += 1\n            max_count = max(max_count, current_count)\n        else:\n            current_count = 0\n\n    return max_count\n\"\"\",\n\"tests\": [\n    \"assert max_consecutive_ones('10101010') == 5\",\n    \"assert max_consecutive_ones('01101011') == 3\",\n    \"assert max_consecutive_ones('1000001') == 2\",\n    \"assert max_consecutive_ones('0') == 0\"\n]\n}"