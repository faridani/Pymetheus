"Here is the problem I came up with:\n\n{\"title\": \"Euler's Totient Function\", \"description\": \"The Euler's totient function, denoted as \u03c6(n), is a mathematical function that counts the positive integers up to a given number n that are relatively prime to n. It is used in number theory and has applications in cryptography.\n\nWrite a function `euler_totient` that calculates the Euler's totient function for a given integer `n`.\n\n#### Function Signature\n```python\ndef euler_totient(n: int) -> int:\n```\n\n#### Input\n- `n` (int): A positive integer.\n\n#### Output\n- Returns the value of the Euler's totient function \u03c6(n).\n\n\", \"code\": \"\nimport math\n\ndef gcd(a: int, b: int) -> int:\n    while b:\n        a, b = b, a % b\n    return a\n\ndef euler_totient(n: int) -> int:\n    result = n\n    p = 2\n    while p * p <= n:\n        if n % p == 0:\n            while n % p == 0:\n                n //= p\n            result -= result // p\n        p += 1\n    if n > 1:\n        result -= 1\n    return result\n\n\", \"tests\": [\"assert euler_totient(1) == 1\", \"assert euler_totient(2) == 1\", \"assert euler_totient(3) == 2\", \"assert euler_totient(4) == 2\", \"assert euler_totient(5) == 4\", \"assert euler_totient(6) == 2\", \"assert euler_totient(7) == 6\", \"assert euler_totient(8) == 4\", \"assert euler_totient(9) == 6\"]}"