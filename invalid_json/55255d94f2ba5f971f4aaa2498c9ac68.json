"Here's another Python coding question that I came up with:\n\n\n{'title': 'Easy: Max Consecutive Ones in Binary Matrix', \n'description': 'A binary matrix is an `m x n` matrix, where each cell contains either 0 or 1. Given a binary matrix write a function that returns the maximum number of consecutive ones in any row.\\n\\n#### Function Signature\\n```python\\ndef max_consecutive_ones(matrix: list[list[int]]) -> int:\\n```\\n\\n#### Input\\n- `matrix` (list[list[int]]): A 2D list containing binary integers.\\n\\n#### Output\\n- Returns the maximum number of consecutive ones in any row.\\n', \n'code': \"def max_consecutive_ones(matrix: list[list[int]]) -> int:\\n    # Initialize maximum consecutive ones with a value less than any possible result\\n    max_consecutive = 0\\n    # Iterate over each row in the matrix\\n    for row in matrix:\\n        # Initialize current consecutive ones to 0\\n        current_consecutive = 0\\n        # Iterate over each element in the row\\n        for num in row:\\n            # If the number is one, increment current consecutive ones\\n            if num == 1:\\n                current_consecutive += 1\\n            # Otherwise, reset current consecutive ones to zero\\n            else:\\n                current_consecutive = 0\\n        # Update maximum consecutive ones if necessary\\n        max_consecutive = max(max_consecutive, current_consecutive)\\n    # Return the maximum number of consecutive ones in any row\\n    return max_consecutive\\n\", \n'tests': ['assert max_consecutive_ones([[1, 1, 0], [1, 1, 1]]) == 3', 'assert max_consecutive_ones([[1, 0, 1], [1, 0, 1]]) == 2', 'assert max_consecutive_ones([[], []]) == 0']}"