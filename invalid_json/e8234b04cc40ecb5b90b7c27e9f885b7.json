"Here's a new Python coding question for you:\n\n{\"title\": \"Easy: Find the First Duplicate\", \n\"description\": \"Write a function that takes an array of integers as input and returns the first duplicate it finds. If no duplicates are found, return -1.\\n\\n#### Function Signature\\n```python\\ndef find_first_duplicate(arr: list[int)) -> int:\\n```\\n\\n#### Input\\n- `arr` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the first duplicate integer in the array. If no duplicates are found, returns -1.\\n\", \n\"code\": \"def find_first_duplicate(arr: list[int)) -> int:\\n    seen = set()\\n    for num in arr:\\n        if num in seen:\\n            return num\\n        seen.add(num)\\n    return -1\\n\", \n\"tests\": [\"assert find_first_duplicate([1, 2, 3, 4, 5]) == -1\", \"assert find_first_duplicate([1, 2, 3, 2, 4]) == 2\", \"assert find_first_duplicate([1, 2, 1]) == 1\"]}"