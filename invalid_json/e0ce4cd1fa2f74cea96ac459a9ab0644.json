"Here is the python coding question:\n\n{\"title\": \"Easy: Counting Duplicates\", \n\"description\": \"Write a function that takes a list of integers as an input and returns the count of duplicate elements in the list.\\n\\n#### Function Signature\\n```python\\ndef count_duplicates(lst: list[int]) -> int:\\n```\\n\\n#### Input\\n- `lst` (list[int]): A list of integers.\\n\\n#### Output\\n- Returns the count of duplicate elements in the input list.\\n\", \n\"code\": \"def count_duplicates(lst: list[int]) -> int:\\n    # Initialize a dictionary to store the frequency of each element\\n    freq_dict = {}\\n    for num in lst:\\n        if num in freq_dict:\\n            freq_dict[num] += 1\\n        else:\\n            freq_dict[num] = 1\\n    # Count the duplicate elements by subtracting the count of unique elements from the total count\\n    return len(lst) - sum(1 for k, v in freq_dict.items() if v == 1)\\n\", \n\"tests\": [\"assert count_duplicates([1, 2, 3, 4, 5]) == 0\", \"assert count_duplicates([1, 2, 2, 3, 3, 3]) == 2\", \"assert count_duplicates([]) == 0\", \"assert count_duplicates([1, 2, 3, 4, 5, 1]) == 1\"]}"