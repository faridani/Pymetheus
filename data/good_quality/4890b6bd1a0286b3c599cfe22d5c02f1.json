{
    "title": "\"Anagram Finder (Hard)",
    "description": "Given two strings, write a function `anagram_checker` that checks if the two input strings are anagrams of each other.\n\n#### Function Signature\n```python\ndef anagram_checker(s1: str, s2: str) -> bool:\n```\n\n#### Input\n- `s1` (str): A string containing only lowercase letters.\n- `s2` (str): Another string containing only lowercase letters.\n\n#### Output\n- Returns `True` if the two input strings are anagrams of each other, `False` otherwise.\n\nNote that an anagram is a word or phrase that consists of the same letters as another, but in a different order.",
    "code": "def anagram_checker(s1: str, s2: str) -> bool:\n    # Sort both strings and compare them\n    return sorted(s1) == sorted(s2)",
    "tests": [
        "assert anagram_checker(\"listen\", \"silent\") == True",
        "assert anagram_checker(\"hello\", \"world\") == False",
        "assert anagram_checker(\"oathkeeper\", \"peacekeeper\") == True"
    ],
    "difficulty": "insanely difficult",
    "model": "mistral:latest",
    "style": "Codeforces"
}