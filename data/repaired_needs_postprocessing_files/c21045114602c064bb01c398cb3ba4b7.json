{
    "title": "Anagram Finder (Hard)",
    "description": "Given two strings, write a function `anagram_checker` that checks if the two input strings are anagrams of each other.\n\n#### Function Signature\n```python\ndef anagram_checker(str1: str, str2: str) -> bool:\n``` \n\n#### Input\n- `str1`, `str2` (str): Two strings containing only lowercase English alphabets without spaces.\n\n#### Output\n- Returns `True` if the two input strings are anagrams of each other, `False` otherwise.\n",
    "code": "def anagram_checker(str1: str, str2: str) -> bool:\n    # Convert both strings to sorted lists of characters\n    list1 = sorted(list(str1))\n    list2 = sorted(list(str2))\n    \n    # Check if the two sorted lists are equal\n    return list1 == list2",
    "tests": [
        "assert anagram_checker('listen', 'silent') == True",
        "assert anagram_checker('hello', 'world') == False",
        "assert anagram_checker('act', 'cat') == True",
        "assert anagram_checker('python', 'typhon') == False"
    ],
    "model": "mistral:latest-repaired by Llama 3"
}